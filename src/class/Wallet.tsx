// Wallet is now a plain data interface — see src/types/wallet.types.ts.
// This file is kept for import compatibility during the migration.
export type { WalletData as default } from '../types/wallet.types';
