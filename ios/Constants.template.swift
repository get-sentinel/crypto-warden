// Copy this file to Constants.swift and fill in your own values.
// Constants.swift is gitignored — never commit it.

import Foundation

enum Constants {
    static let keychainID = "your.bundle.id.here"
}
